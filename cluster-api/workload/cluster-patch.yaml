# This file can be used to patch cluster configuration
# Example patches:

# apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
# kind: AzureManagedControlPlane
# metadata:
#   name: aks-workload-cluster
#   namespace: default
# spec:
#   version: v1.29.0  # Override Kubernetes version
#   
# ---
# apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
# kind: AzureManagedMachinePool
# metadata:
#   name: agentpool0
#   namespace: default
# spec:
#   sku: Standard_D4s_v3  # Override VM size
